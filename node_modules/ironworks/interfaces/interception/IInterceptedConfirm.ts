
import ICommEmit = require('../eventing/ICommEmit');

interface IInterceptedConfirm {
    (   emit: ICommEmit,
        next: (callback: (e: Error) => void) => void,
        stop: () => void
    ): void;
}

export = IInterceptedConfirm;
